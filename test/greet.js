function greet(name){
  var name = '';
    return('Hello' + ', ' + name);

  };

  var greeting = greet(name);
  
 
    function Language() {
        if (Language = english)
        return "Hello";
    }
        if(Language = Xhosa){
            return "Molo";
        }

        var greeting = greet(name);



  